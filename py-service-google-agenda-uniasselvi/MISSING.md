# Pendências para publicação no GitHub

Status atual: não há arquivo de entrada (`main.py`/`app.py`) no root.

O que falta:

- Exemplo de `main.py` com fluxo de autenticação e chamadas à Google Agenda.
- `requirements.txt` com dependências (google-api-python-client, oauthlib etc.).
- Documentar como obter credenciais e definir variáveis de ambiente.

Próximos passos sugeridos:

1. Criar `main.py` com exemplo mínimo.
2. Gerar `requirements.txt`.
3. Atualizar `README.md` e não commitar credenciais.
4. Commitar e enviar para o GitHub.