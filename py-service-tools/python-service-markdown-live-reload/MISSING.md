# Pendências para publicação no GitHub

Status atual: não há arquivo de entrada no root (`main.py` ou `app.py`).

O que falta:

- Adicionar um arquivo de entrada (ex.: `main.py`) com um pequeno exemplo de execução.
- Incluir `requirements.txt` com dependências usadas.
- Verificar e documentar variáveis de ambiente, se houver.
- Testar localmente e atualizar este README com instruções de uso.

Próximos passos sugeridos:

1. Criar `main.py` com um `if __name__ == "__main__":` e um exemplo simples.
2. Executar `pip freeze > requirements.txt` em um ambiente limpo.
3. Preencher o `README.md` com comandos de instalação e execução.
4. Commitar e abrir PR/Push para o GitHub.
