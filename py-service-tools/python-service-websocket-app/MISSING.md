# Pendências para publicação no GitHub

Status atual: não há `main.py`/`app.py` no root.

O que falta:

- Criar `main.py` que inicie o servidor WebSocket.
- `requirements.txt` com `websockets` ou dependências usadas.

Próximos passos sugeridos:

1. Implementar `main.py` com um exemplo de cliente/servidor.
2. Adicionar `requirements.txt`.
3. Atualizar `README.md`.