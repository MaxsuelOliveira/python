# Pendências para publicação no GitHub

Status atual: não há `main.py`/`app.py` no root.

O que falta:

- Criar `main.py` que demonstre uso do ngrok para expor serviços locais.
- `requirements.txt` e instruções sobre como usar ngrok (token, comando CLI).

Próximos passos sugeridos:

1. Adicionar `main.py` ou um exemplo de scripts.
2. Adicionar `requirements.txt`.
3. Atualizar `README.md`.