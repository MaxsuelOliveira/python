# Pendências para publicação no GitHub

Status atual: não há `main.py`/`app.py` no root (submódulos possuem exemplos).

O que falta:

- `main.py` orientador no root para executar cada exemplo.
- `requirements.txt` e instruções de configuração para modelos.

Próximos passos sugeridos:

1. Criar `main.py` explicando submódulos.
2. Adicionar `requirements.txt`.
3. Atualizar `README.md`.