# Pendências para publicação no GitHub

Status atual: não há `main.py`/`app.py` no root (os submódulos têm `main.py`).

O que falta:

- Criar um `main.py` no root que documente/roteirize os submódulos e explique como executar cada exemplo.
- `requirements.txt` consolidado com dependências (OpenCV etc.).

Próximos passos sugeridos:

1. Adicionar `main.py` explicativo.
2. Unificar dependências em `requirements.txt`.
3. Atualizar `README.md`.