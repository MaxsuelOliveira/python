# Pendências para publicação no GitHub

Status atual: não há `main.py`/`app.py` no root.

O que falta:

- Criar `main.py` que sirva/mostre o front-end (index.html) se for um servidor simples, ou descrever o fluxo de deploy.
- `requirements.txt` (se houver backend em Python) ou instruções de build para front-end.

Próximos passos sugeridos:

1. Adicionar um script de start (`main.py` ou `server/README.md`).
2. Documentar como servir arquivos estáticos.
3. Atualizar `README.md`.